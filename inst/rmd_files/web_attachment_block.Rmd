## Attachments

```{r, results='asis', echo = FALSE, eval=knitr::is_html_output()}

f = list.files(
	"files", 
	recursive    = TRUE, 
	include.dirs = FALSE
	)

sprintf(
		"* [%s](%s)",
		f,
		file.path("files", f)
		) |> 
	cat(sep = "\n")
```
